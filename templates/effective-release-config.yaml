apiVersion: agentfactory.io/v1alpha1
kind: EffectiveReleaseConfig

metadata:
  releaseId: release-001
  environment: sandbox

policy:
  platformPolicyName: platform-default
  platformPolicyVersion: "1"
  exceptionPolicyRefs: []

spec:
  agentRef:
    name: example-agent
    version: 0.1.0
  tenant:
    id: example-tenant
  variables: {}
  capabilities:
    provides: []
    requires: []
  capabilityBindings: {}
  trustProfile: internal
  releaseStrategy: policy-auto
  providerProfile: balanced
  secretsRef: {}
  memoryConfig: {}
  budgetConfig: {}
  permissions: []
  dataClassification: internal
  toolBindings: {}
  evalProfile: standard-agent

# Generated by the Core compiler.
# This is the only runtime-executable configuration artifact.
# trustProfile is the compiled runtime trust authority for this release.
# releaseStrategy is already concrete: policy-auto or human-required.
# Capability bindings are resolved from the authoritative Registry.
# Do not hand-edit a released EffectiveReleaseConfig; generate a new release instead.
