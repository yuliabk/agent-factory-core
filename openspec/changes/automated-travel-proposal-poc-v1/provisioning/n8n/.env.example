# Copy to an ignored .env only after the host prerequisites pass.
# Values are paths only. Never place key or certificate contents in this file.
# All referenced files must be outside the repository and the n8n data volume.
N8N_ENCRYPTION_KEY_FILE_PATH=
N8N_TLS_KEY_FILE_PATH=
N8N_TLS_CERT_FILE_PATH=
